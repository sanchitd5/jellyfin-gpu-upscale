# build output
bin/
obj/
out/
out-patcher/
*.dll
*.pdb
*.nupkg

# shaders fetched at install time (not redistributed)
shaders/FSRCNNX_*.glsl
shaders/FSRCNN_*.glsl
shaders/Anime4K_*.glsl

# web build tooling (npm install; package-lock.json IS checked in)
web/node_modules/

# web/gpu-upscale.js is a BUILD OUTPUT (esbuild, from web/src/); rebuild with
# scripts/build-web-panel.sh, never hand-edit or commit it
web/gpu-upscale.js

# local
.DS_Store
*.swp
__pycache__/
*.pyc
livetestbox.md

# user-supplied NVIDIA driver/SDK DLLs for local investigation and future
# filters (nvdlppx.dll, nvaivpx.dll, nvngx_dlssnr.dll, its forwarder, etc.) -
# already covered by *.dll above, called out explicitly here since this is
# the conventional local drop location. Never fetched or committed by us:
# see DLSS.md / RTXDLPP.md / roadmap/dlss-neural-rendering.md.
/dll/

# local clones of third-party repos for reading source during investigation -
# never vendored/committed here.
/researchRepos/
